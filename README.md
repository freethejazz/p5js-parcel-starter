## P5.js + ES6 via Parcel

To run this, first install [Parcel](https://parceljs.org/) globally:

```
yarn global add parcel-bundler
# or
npm install -g parcel-bundler
```

Then simply run:
```
parcel index.html
```
